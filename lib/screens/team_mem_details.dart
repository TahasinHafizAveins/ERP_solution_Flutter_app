import 'package:flutter/material.dart';

class TeamMemDetails extends StatelessWidget {
  const TeamMemDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
