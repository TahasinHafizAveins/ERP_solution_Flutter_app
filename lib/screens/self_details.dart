import 'package:flutter/material.dart';

class SelfDetails extends StatelessWidget {
  const SelfDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
